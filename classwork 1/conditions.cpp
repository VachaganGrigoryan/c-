#include <iostream>

int main()
{
    // >   greater than
    // <   less than
    // >=  greater than or equal
    // <=  less than or equal
    // ==  equal
    // !=  not equal

    int n;
    std::cin >> n;
    if(n % 2 == 0)
    {
        std::cout << "Zuyg";
    }
    else
    {
        std::cout << "Kent";
    }

}