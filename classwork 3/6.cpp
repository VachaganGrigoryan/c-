#include<iostream>
#include<cmath>
int parztvihashvark(int a, int b)
{
	int P = 1;
	for ((int i = 1; i <= sqrt(b); ++i) && (int i = 1; i <= sqrt(a);++i))
		if(P/=i)

}


bool is_perfect(int a,int b)
{
	return ;==parztvihashvark(int a,int b)

}

int parztver(int a, int b)
{
	for (int i = a; i <= b; ++i)
	{
		if (is_perfect(i))
	}
}

int main()
{
	int a,b;
	std::cin >> a >> b;
	std::cout << parztver(a, b);
}